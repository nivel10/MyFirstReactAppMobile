import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function InfoUser({ user, }) {
    return (
        <View>
            <Text>Info user (Component)</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
